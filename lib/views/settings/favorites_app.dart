import 'package:flutter/material.dart';

class FavoritesApp extends StatelessWidget {
  const FavoritesApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('تفضيلات التطبيق'));
  }
}
