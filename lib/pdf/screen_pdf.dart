import 'package:flutter/material.dart';

class ScreenPdf extends StatelessWidget {
  const ScreenPdf({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('الطباعة')),
      body: const Center(child: null),
    );
  }
}
